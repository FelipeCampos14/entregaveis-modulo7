version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_PASSWORD: postgres_felipe
      POSTGRES_USER: postgres_felipe
      POSTGRES_DB: postgres_felipe
    ports:
      - 5432:5432
    container_name: postgres_felipe